# CHANGELOG

기능을 고칠 때마다 맨 위에 한 덩어리씩 쌓는다. 날짜 · 버전 · 무엇을 · 왜.

## 0.1.0 — 2026-09-10

첫 버전. 구글 설문 4년 치를 걷어내고 GitHub Pages + Supabase 로 옮겼다.

**추가**
- 로그인(Supabase Auth) 후 사람 전환. 두 사람이 서로 기록을 보고 대신 입력할 수 있다.
- 기록 화면: 날짜 이동 → 종목 타일 → 그 종목 칸만 뜨는 시트. 종목 하나만 입력할 때 다른 칸을 지나가지 않는다.
- 개수 입력에 `+10 / +20 / +50 / +100` 빠른 버튼.
- 시간은 분·초 두 칸으로 직접 입력. 초→소수 변환표를 없앴다.
- 하루 단위 몸무게 · 메모 · "오늘은 쉬는 날".
- 대시보드 3종: 최근 14일 꺾은선(종목 칩 전환, 2지표 이중 축) / 기간별 도넛·합계표·몸무게 추이 / 종목별 역대·최근 90일 최고기록(달리기·사이클은 페이스 포함).
- 매뉴얼 탭 — 인스타 링크 8개.
- 종목 추가를 `js/data.js` 한 줄로 끝내는 구조. 풀업·배영·평영·등산·사이클·배드민턴 미리 등록(배영·평영은 `active:false`).

**데이터 이관**
- 원본 1,267행(2022-06-11 ~ 2026-09-04) → 운동 세션 1,873행 + 하루단위 335행.
- `팔`→`pushup`, `배`→`core`, `허벅지`→`leg`, `수영`→`swim_free`.
- `기타` 11건은 비고 텍스트로 종목 복원(배드민턴 4 · 사이클 4 · 대회 3).
- 소수 분 인코딩(`47.6`)을 초로 환산. 비고 267건, 몸무게 59건 전부 살렸다.
- 쉰 날 165일을 `is_rest` 로 보존.

**만들면서 잡은 것**
- `signIn()` 이 Promise 를 그대로 검사기에 넘겨 **비밀번호가 틀려도 에러가 안 잡히던** 문제.
- 조건부 자식을 `replaceChildren` 에 넘겨 화면에 `null` 글자가 찍히던 문제 → `mount()` 헬퍼로 통일.
- Chart.js 캔버스가 높이 0px 으로 무너지던 문제 → `.canvas-wrap` 래퍼 도입.
- 4년 누적 달리기 시간이 `25013분` 으로 나오던 것 → `fmtDur()` 로 `416시간 53분` 표기.

**하지 않은 것**
- PWA·오프라인 캐시(고쳐도 안 바뀌는 사고가 잦다), 번들러(github.dev 배포 경로가 죽는다), 서버측 집계 뷰(종목 추가마다 DB를 손대게 된다).
